import assert from 'node:assert';
import test, { describe, it } from 'node:test';

describe('transações entre clientes', () => {
  it("Verifica se cliente existe", () => {
    assert.equal(true, true)
  })
})